import React from 'react';
import { Input } from 'antd';

const { TextArea } = Input;

const TextArea = () => {
  return (
    <div>
      <TextArea rows={4} />
    </div>
  );
}

export default TextArea
